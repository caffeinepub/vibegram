import type { backendInterface } from "../backend";
import {
  MediaType,
  NotificationType,
  UserRole,
  WalletTxStatus,
  WalletTxType,
  WithdrawalStatus,
} from "../backend";
import { Principal } from "@icp-sdk/core/principal";

const mockPrincipal = Principal.fromText("2vxsx-fae");

export const mockBackend: backendInterface = {
  _initializeAccessControl: async () => undefined,
  adminApproveWithdrawal: async () => undefined,
  adminGetAnalytics: async () => ({
    pendingWithdrawals: BigInt(3),
    totalUsers: BigInt(1420),
    totalWithdrawals: 5200,
    totalPosts: BigInt(8430),
    newUsersThisWeek: BigInt(87),
    totalReels: BigInt(2310),
  }),
  adminGetFlaggedPosts: async () => [],
  adminGetPosts: async () => [
    {
      id: BigInt(1),
      likeCount: BigInt(42),
      authorId: mockPrincipal,
      createdAt: BigInt(Date.now()),
      caption: "Beautiful sunset in Mumbai 🌅",
      commentCount: BigInt(7),
      mediaType: MediaType.photo,
      isFlagged: false,
      flagCount: BigInt(0),
    },
  ],
  adminGetReferralStats: async () => ({
    totalReferrals: BigInt(320),
    totalPaid: 12500,
    topReferrers: [
      { username: "spandan", earnings: 2400 },
      { username: "rahul_v", earnings: 1800 },
      { username: "priya_k", earnings: 1500 },
    ],
    pendingPayout: 800,
  }),
  adminGetUsers: async () => [
    {
      postCount: BigInt(12),
      username: "spandan",
      displayName: "Spandan Dev",
      userId: mockPrincipal,
      joinedAt: BigInt(Date.now() - 86400000 * 30),
      isActive: true,
      isSuspended: false,
      followerCount: BigInt(340),
    },
    {
      postCount: BigInt(5),
      username: "rahul_v",
      displayName: "Rahul Verma",
      userId: mockPrincipal,
      joinedAt: BigInt(Date.now() - 86400000 * 15),
      isActive: true,
      isSuspended: false,
      followerCount: BigInt(120),
    },
  ],
  adminGetAllUsers: async () => [
    {
      postCount: BigInt(12),
      username: "spandan",
      displayName: "Spandan Dev",
      userId: mockPrincipal,
      joinedAt: BigInt(Date.now() - 86400000 * 30),
      isActive: true,
      isSuspended: false,
      followerCount: BigInt(340),
    },
  ],
  adminGetAuditLog: async () => [
    {
      id: BigInt(1),
      action: "REMOVE_POST",
      timestamp: BigInt(Date.now() - 3600000),
      details: "Removed post 42",
      adminId: mockPrincipal,
    },
  ],
  adminGetFraudScores: async () => [
    {
      userId: mockPrincipal,
      username: "suspicious_user",
      riskScore: BigInt(75),
      flags: ["same_ip_referral", "rapid_signups"],
    },
  ],
  adminGetRewards: async () => ({
    signupBonus: 10,
    reelBonus: 20,
    followerBonus: 50,
  }),
  adminSetRewards: async () => true,
  adminGetWithdrawalRequests: async () => [
    {
      id: BigInt(1),
      status: WithdrawalStatus.pending,
      method: "upi",
      userId: mockPrincipal,
      createdAt: BigInt(Date.now()),
      amount: 500,
      accountDetails: "spandan@upi",
    },
  ],
  adminRejectWithdrawal: async () => undefined,
  adminRemovePost: async () => undefined,
  adminSearchUsers: async () => [],
  adminSetAdmin: async () => undefined,
  adminSuspendUser: async () => undefined,
  adminUnsuspendUser: async () => undefined,
  approveWithdrawal: async () => undefined,
  assignCallerUserRole: async () => undefined,
  createComment: async (_postId, text) => ({
    authorId: mockPrincipal,
    createdAt: BigInt(Date.now()),
    text,
    postId: BigInt(1),
  }),
  createPost: async (mediaUrl, mediaType, caption) => ({
    id: BigInt(99),
    authorId: mockPrincipal,
    createdAt: BigInt(Date.now()),
    tags: [],
    mediaUrl,
    caption,
    mediaType,
    location: undefined,
  }),
  flagPost: async () => undefined,
  followUser: async () => undefined,
  getCallerUserProfile: async () => ({
    bio: "Living life through the lens 📸",
    username: "spandan",
    displayName: "Spandan Dev",
    socialLinks: ["https://github.com/spandan"],
    profilePhoto: undefined,
    pronouns: "he/him",
  }),
  getCallerUserRole: async () => UserRole.user,
  getConversation: async () => [
    {
      id: BigInt(1),
      createdAt: BigInt(Date.now() - 60000),
      text: "Hey! Check out my new reel 🔥",
      receiverId: mockPrincipal,
      senderId: mockPrincipal,
    },
    {
      id: BigInt(2),
      createdAt: BigInt(Date.now()),
      text: "That's awesome! Keep it up 🙌",
      receiverId: mockPrincipal,
      senderId: mockPrincipal,
    },
  ],
  getExploreFeed: async () => [
    {
      id: BigInt(2),
      authorId: mockPrincipal,
      createdAt: BigInt(Date.now() - 3600000),
      tags: ["travel", "india"],
      mediaUrl: "https://picsum.photos/seed/explore1/400/400",
      caption: "Golden hour at the ghats 🌊 #travel #india",
      mediaType: MediaType.photo,
      location: "Varanasi, India",
    },
    {
      id: BigInt(3),
      authorId: mockPrincipal,
      createdAt: BigInt(Date.now() - 7200000),
      tags: ["fashion"],
      mediaUrl: "https://picsum.photos/seed/explore2/400/400",
      caption: "New fit drop 🔥 #fashion",
      mediaType: MediaType.photo,
      location: undefined,
    },
  ],
  getFollowers: async () => [],
  getFollowing: async () => [],
  getHomeFeed: async () => [
    {
      id: BigInt(1),
      authorId: mockPrincipal,
      createdAt: BigInt(Date.now() - 1800000),
      tags: ["sunset", "mumbai"],
      mediaUrl: "https://picsum.photos/seed/feed1/400/500",
      caption: "Beautiful evening vibes 🌅 #sunset #mumbai",
      mediaType: MediaType.photo,
      location: "Mumbai, India",
    },
    {
      id: BigInt(4),
      authorId: mockPrincipal,
      createdAt: BigInt(Date.now() - 5400000),
      tags: ["food"],
      mediaUrl: "https://picsum.photos/seed/feed2/400/500",
      caption: "Chai pe charcha ☕ #food",
      mediaType: MediaType.photo,
      location: undefined,
    },
  ],
  getMyWallet: async () => ({
    balance: 1250,
    transactions: [
      {
        id: BigInt(1),
        status: WalletTxStatus.completed,
        userId: mockPrincipal,
        description: "Referral signup bonus – @rahul_v joined",
        timestamp: BigInt(Date.now() - 86400000),
        txType: WalletTxType.referralSignup,
        amount: 10,
      },
      {
        id: BigInt(2),
        status: WalletTxStatus.completed,
        userId: mockPrincipal,
        description: "Referral reel reward – @rahul_v posted first reel",
        timestamp: BigInt(Date.now() - 43200000),
        txType: WalletTxType.referralReel,
        amount: 20,
      },
      {
        id: BigInt(3),
        status: WalletTxStatus.completed,
        userId: mockPrincipal,
        description: "Referral milestone – @priya_k reached 100 followers",
        timestamp: BigInt(Date.now() - 21600000),
        txType: WalletTxType.referralFollowers,
        amount: 50,
      },
      {
        id: BigInt(4),
        status: WalletTxStatus.pending,
        userId: mockPrincipal,
        description: "Withdrawal request – UPI",
        timestamp: BigInt(Date.now() - 3600000),
        txType: WalletTxType.withdrawal,
        amount: -500,
        withdrawalMethod: { __kind__: "upi", upi: "spandan@upi" },
      },
    ],
  }),
  getNotifications: async () => [
    {
      id: BigInt(1),
      createdAt: BigInt(Date.now() - 60000),
      read: false,
      type: NotificationType.referralSignup,
      actorId: mockPrincipal,
      message: "₹10 earned! @rahul_v signed up using your referral code.",
      recipientId: mockPrincipal,
    },
    {
      id: BigInt(2),
      createdAt: BigInt(Date.now() - 120000),
      read: false,
      type: NotificationType.like,
      actorId: mockPrincipal,
      message: "@priya_k liked your post",
      recipientId: mockPrincipal,
    },
    {
      id: BigInt(3),
      createdAt: BigInt(Date.now() - 3600000),
      read: true,
      type: NotificationType.referralReel,
      actorId: mockPrincipal,
      message: "₹20 earned! @rahul_v posted their first reel.",
      recipientId: mockPrincipal,
    },
    {
      id: BigInt(4),
      createdAt: BigInt(Date.now() - 7200000),
      read: true,
      type: NotificationType.follow,
      actorId: mockPrincipal,
      message: "@amit_sharma started following you",
      recipientId: mockPrincipal,
    },
  ],
  getPost: async () => ({
    id: BigInt(1),
    authorId: mockPrincipal,
    createdAt: BigInt(Date.now() - 1800000),
    tags: ["sunset"],
    mediaUrl: "https://picsum.photos/seed/feed1/400/500",
    caption: "Beautiful evening vibes 🌅",
    mediaType: MediaType.photo,
    location: "Mumbai, India",
  }),
  getPostComments: async () => [
    {
      authorId: mockPrincipal,
      createdAt: BigInt(Date.now() - 300000),
      text: "Stunning! 🔥",
      postId: BigInt(1),
    },
  ],
  getPostLikes: async () => [],
  getRecentConversations: async () => [mockPrincipal],
  getReferralStats: async () => ({
    referralCode: "BUTKI-SPD42",
    totalReferrals: BigInt(8),
    totalEarned: 320,
  }),
  getUserPosts: async () => [
    {
      id: BigInt(1),
      authorId: mockPrincipal,
      createdAt: BigInt(Date.now() - 1800000),
      tags: [],
      mediaUrl: "https://picsum.photos/seed/prof1/300/300",
      caption: "Post 1",
      mediaType: MediaType.photo,
      location: undefined,
    },
    {
      id: BigInt(2),
      authorId: mockPrincipal,
      createdAt: BigInt(Date.now() - 3600000),
      tags: [],
      mediaUrl: "https://picsum.photos/seed/prof2/300/300",
      caption: "Post 2",
      mediaType: MediaType.photo,
      location: undefined,
    },
  ],
  getUserProfile: async () => ({
    bio: "Living life through the lens 📸",
    username: "spandan",
    displayName: "Spandan Dev",
    socialLinks: ["https://github.com/spandan"],
    profilePhoto: undefined,
    pronouns: "he/him",
  }),
  getWithdrawalRequests: async () => [],
  isAdmin: async () => false,
  isCallerAdmin: async () => false,
  markNotificationsAsRead: async () => undefined,
  registerUser: async (username, displayName) => ({
    bio: "",
    username,
    displayName,
    socialLinks: [],
    profilePhoto: undefined,
    pronouns: undefined,
  }),
  rejectWithdrawal: async () => undefined,
  requestWithdrawal: async () => ({
    __kind__: "ok",
    ok: {
      id: BigInt(5),
      status: WalletTxStatus.pending,
      userId: mockPrincipal,
      description: "Withdrawal request",
      timestamp: BigInt(Date.now()),
      txType: WalletTxType.withdrawal,
      amount: -100,
    },
  }),
  saveCallerUserProfile: async () => undefined,
  searchUsers: async () => [
    {
      bio: "Photo enthusiast",
      username: "priya_k",
      displayName: "Priya Kapoor",
      socialLinks: [],
      profilePhoto: undefined,
      pronouns: undefined,
    },
  ],
  sendMessage: async (_receiverId, text) => ({
    id: BigInt(99),
    createdAt: BigInt(Date.now()),
    text,
    receiverId: mockPrincipal,
    senderId: mockPrincipal,
  }),
  toggleLike: async () => true,
  unfollowUser: async () => undefined,
  updateProfile: async (displayName, bio) => ({
    bio,
    username: "spandan",
    displayName,
    socialLinks: [],
    profilePhoto: undefined,
    pronouns: undefined,
  }),
};
